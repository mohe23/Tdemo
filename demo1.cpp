#include<graphics.h>
#include<Windows.h>

/*
int main(){
    char ch[]="无码高清";
    initgraph(640,480);//定义“画布”大小

    setbkcolor(RGB(64,128,128));//设置背景色
    cleardevice();//用当前设置的背景色清空屏幕。

    setlinecolor(RGB(250,0,0));//设置画笔的颜色
    setlinestyle(PS_SOLID,10);//设置画笔的样式，“PS_SOLID”表示实线。“10”表示线宽
    circle(320,240,200);//画出图形的第1部分“圆”。

    settextcolor(RGB(250,250,0));//设置字体的颜色
    settextstyle(100,0,"微软雅黑");//设置字体，从左到右，依次代表：字体的高度，字体的宽度，字体
    outtextxy(170,190,ch);//在位置（170,190）打印“无码高清”四个字 。完成图形的第2部分。

    line(180,380,460,100);//画线，从位置（180,380）到位置（460,100）画一条线。完成图形的第3部分，即最后一部分。

    system("pause");//主要功能，让生成的图形“暂停”一下，不是生成之后就消失了
    closegraph();//记得关闭图像
return 0;
}
*/
