#include<graphics.h>//引入EasyX图形库的头文件
#include<Windows.h>//为了使用system("pause");

/*
int main(){
    IMAGE a;//声明1个图片类型的变量
    loadimage(&a,"D:\\125.jpg");//向这个图片变量a中导入图片。
    initgraph(800,1000);//定义“绘图窗口”大小
    putimage(0,0,&a);//将图片打印到绘图窗口中

    system("pause");//暂停程序，使绘图窗口一直出现在屏幕上。

    closegraph();//关闭图像

return 0;
}
*/
