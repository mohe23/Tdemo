#include <iostream>
#include<graphics.h>
#include<Windows.h>
using namespace std;

/*
int main()
{
    initgraph(640,480);
    setlinecolor(RGB(0,0,245));//设置画笔颜色“蓝色”
    setlinestyle(PS_DASH,5);//设置画笔格式，虚线，线宽5
    circle(320,240,100);//画1个圆

    setlinecolor(RGB(0,255,0));//设置画笔颜色-绿色
    setlinestyle(PS_SOLID,10);//设置画笔格式，实线，线宽10

    line(30,30,30,400);//矩形的上面一条线
    line(600,30,600,400);//矩形下面一条线
    line(30,30,600,30);//矩形左边一条线
    line(30,400,600,400);//矩形右边一条线

    setlinecolor(RGB(255,0,0));//设置画笔颜色

   //三角形  (10,240)  (580,50)  (600,430)
   //坐标（10,240）中的“10”：表示该点距离绘图窗口最左边的距离；“240”：该点距离绘图窗口最上边的距离
   line(10,240,580,50);//三角形一条边
   line(10,240,600,430);//三角形一条边
   line(580,50,600,430);//三角形一条边

    //line(0,0,640,480);//第1条线
    //line(640,0,0,480);//第2条线
    system("pause");//使程序暂停
    closegraph();
    //cout << "Hello world!" << endl;
    return 0;
}
*/
